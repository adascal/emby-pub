name: Build and Release

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'

    - name: Restore dependencies
      run: dotnet restore Kinopub.Plugin.csproj

    - name: Build
      run: dotnet build Kinopub.Plugin.csproj --configuration Release --no-restore

    - name: Create plugin package
      run: |
        mkdir -p release/Kinopub
        cp bin/Release/net6.0/Kinopub.Plugin.dll release/Kinopub/
        cd release
        zip -r Kinopub-Plugin.zip Kinopub/

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Kinopub-Plugin
        path: release/Kinopub-Plugin.zip

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v1
      with:
        files: release/Kinopub-Plugin.zip
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
